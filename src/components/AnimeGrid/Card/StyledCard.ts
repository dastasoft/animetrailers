import styled from 'styled-components'

export const StyledCard = styled.div`
  position: relative;
  cursor: pointer;
  flex: 1;

  img {
    border-radius: 0.25rem;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
`
